#include"Header.h"

int main()
{
	srand(time(NULL));
	/*int n = 1000;
	int(*name)(vector<int> &, int);
	name = &bubbleSort;
	vector <int> a;
	a = createdRandom(n);

	for (int i = 0; i < n; i++)
	{
		cout << a[i] << "\t";
	}	cout << endl;

	for (int i = 0; i < n; i++)
	{
		cout << a[i] << "\t";
	}	cout << endl;*/
	//int donvi;
	//saveResults("random", createdRandom, donvi = 1);
	system("pause");
	return 0;
}