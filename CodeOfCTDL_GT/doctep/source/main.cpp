#include"xulytep.h"

//int main()
//{
//	menu();
//	system("pause");
//	return 0;
//}
int main()
{ 
	vector<int> v;
	v.push_back(1);
	v.push_back(1);
	v.push_back(1);
	v.push_back(1);
	v.push_back(1);
	v.insert(++++
		v.begin(), 5);
	for (int i = 0; i < v.size(); i++)
	{ 
		cout << v[i];
	}
	system("pause");
	return 0;
}